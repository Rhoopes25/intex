<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Ella Rises</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('partials/navbar') %>
    <img src="/butterfly.jpg" alt="" class="floating-clipart butterfly-1">
    <img src="/flower2.jpg" alt="" class="floating-clipart flower-hero-left">
    <img src="/flower3.jpg" alt="" class="floating-clipart flower-hero-right">
    
    <div class="page-header">
        <div class="container">
            <h1><i class="fas fa-user me-3"></i>Your Profile</h1>
        </div>
    </div>

    <div class="container mb-5">
        <div class="row">
            <!-- User Info Card -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="profile-header text-center p-4">
                        <div class="profile-icon">
                            <i class="fas fa-user fa-3x"></i>
                        </div>
                        <h3><%= user.userfirstname %> <%= user.userlastname %></h3>
                        <p class="mb-0"><%= user.useremail %></p>
                    </div>
                    <div class="card-body p-4">

                        <!-- Profile Alerts -->
                        <% if (profileError) { %>
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-circle me-2"></i><%= profileError %>
                            </div>
                        <% } %>
                        <% if (profileSuccess) { %>
                            <div class="alert alert-success">
                                <i class="fas fa-check-circle me-2"></i><%= profileSuccess %>
                            </div>
                        <% } %>

                        <!-- Update Username -->
                        <h5 class="mb-3">Update Username</h5>
                        <form method="POST" action="/profile/update-username">
                            <div class="mb-3">
                                <label for="username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="username" name="username" value="<%= user.username %>">
                            </div>
                            <button type="submit" class="btn btn-primary w-100 mb-3">
                                <i class="fas fa-save me-2"></i>Confirm Username
                            </button>
                        </form>

                        <hr>

                        <!-- Update Password -->
                        <h5 class="mb-3">Update Password</h5>
                        <form method="POST" action="/profile/update-password">
                            <div class="mb-3">
                                <label for="currentPassword" class="form-label">Current Password</label>
                                <input type="password" class="form-control" id="currentPassword" name="currentPassword" required>
                            </div>
                            <div class="mb-3">
                                <label for="newPassword" class="form-label">New Password</label>
                                <input type="password" class="form-control" id="newPassword" name="newPassword" required>
                            </div>
                            <div class="mb-3">
                                <label for="confirmPassword" class="form-label">Confirm New Password</label>
                                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required>
                            </div>
                            <button type="submit" class="btn btn-primary w-100 mb-3">
                                <i class="fas fa-key me-2"></i>Update Password
                            </button>
                        </form>

                        <!-- Advanced Settings Button -->
                        <div class="mt-3 text-end">
                            <button type="button" class="btn btn-primary w-100 mb-3" data-bs-toggle="modal" data-bs-target="#advancedSettingsModal">
                                <i class="fas fa-cogs me-2"></i>Advanced Settings
                            </button>
                        </div>

                        <hr>

                        <!-- Delete Account -->
                        <h5 class="mb-3 text-danger">Delete Account</h5>
                        <form method="POST" action="/profile/delete" onsubmit="return confirm('Are you sure you want to delete your account? This action cannot be undone.');">
                            <button type="submit" class="btn btn-outline-danger w-100">
                                <i class="fas fa-trash me-2"></i>Delete Account
                            </button>
                        </form>

                        

                        <!-- Advanced Settings Modal -->
                        <div class="modal fade" id="advancedSettingsModal" tabindex="-1" aria-labelledby="advancedSettingsModalLabel" aria-hidden="true">
                        <div class="modal-dialog modal-lg">
                            <div class="modal-content">
                            <form method="POST" action="/profile/update-participant">
                                <div class="modal-header">
                                <h5 class="modal-title" id="advancedSettingsModalLabel">Edit Participant Info</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">

                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                    <label for="participantdob" class="form-label">Date of Birth</label>
                                    <input type="date" class="form-control" id="participantdob" name="participantdob" value="<%= participant?.participantdob ? participant.participantdob.toISOString().split('T')[0] : '' %>">
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label for="participantphone" class="form-label">Phone Number</label>
                                        <input 
                                            type="tel" 
                                            class="form-control" 
                                            id="participantphone" 
                                            maxlength="14"
                                            name="participantphone" 
                                            value="<%= participant?.participantphone 
                                                     ? participant.participantphone.replace(/(\d{3})(\d{3})(\d{4})/, '($1) $2-$3') 
                                                     : '' %>">
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-5 mb-3">
                                    <label for="participantcity" class="form-label">City</label>
                                    <input type="text" class="form-control" id="participantcity" name="participantcity" value="<%= participant?.participantcity || '' %>">
                                    </div>
                                    <div class="col-md-3 mb-3">
                                    <label for="participantstate" class="form-label">State</label>
                                    <input type="text" class="form-control" id="participantstate" name="participantstate" maxlength="2" value="<%= participant?.participantstate || '' %>">
                                    </div>
                                    <div class="col-md-4 mb-3">
                                    <label for="participantzip" class="form-label">ZIP Code</label>
                                    <input type="text" class="form-control" id="participantzip" name="participantzip" value="<%= participant?.participantzip || '' %>">
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label for="participantschooloremployer" class="form-label">School or Employer</label>
                                    <input type="text" class="form-control" id="participantschooloremployer" name="participantschooloremployer" value="<%= participant?.participantschooloremployer || '' %>">
                                </div>

                                <div class="mb-3">
                                    <label for="participantfieldofinterest" class="form-label">Field of Interest</label>
                                    <select class="form-select" id="participantfieldofinterest" name="participantfieldofinterest">
                                    <option value="">Select a field...</option>
                                    <option value="Science" <%= participant?.participantfieldofinterest === 'Science' ? 'selected' : '' %>>Science</option>
                                    <option value="Technology" <%= participant?.participantfieldofinterest === 'Technology' ? 'selected' : '' %>>Technology</option>
                                    <option value="Engineering" <%= participant?.participantfieldofinterest === 'Engineering' ? 'selected' : '' %>>Engineering</option>
                                    <option value="Arts" <%= participant?.participantfieldofinterest === 'Arts' ? 'selected' : '' %>>Arts</option>
                                    <option value="Mathematics" <%= participant?.participantfieldofinterest === 'Mathematics' ? 'selected' : '' %>>Mathematics</option>
                                    <option value="Other" <%= participant?.participantfieldofinterest === 'Other' ? 'selected' : '' %>>Other</option>
                                    </select>
                                </div>

                                </div>
                                <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-success"><i class="fas fa-save me-2"></i>Save Changes</button>
                                </div>
                            </form>
                            </div>
                        </div>
                        </div>


                    </div>
                </div>
            </div>

            <!-- Events Card -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0"><i class="fas fa-calendar-check me-2"></i>Your Events</h4>
                    </div>
                    <div class="card-body p-4">

                        <!-- Event Alerts -->
                        <% if (eventError) { %>
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-circle me-2"></i><%= eventError %>
                            </div>
                        <% } %>
                        <% if (eventSuccess) { %>
                            <div class="alert alert-success">
                                <i class="fas fa-check-circle me-2"></i><%= eventSuccess %>
                            </div>
                        <% } %>

                        <% if (!participant) { %>
                            <p class="text-muted">No participant information available. Register for an event to get started!</p>
                        <% } else if (events.length === 0) { %>
                            <p class="text-muted">You haven't registered for any events yet.</p>
                            <a href="/events" class="btn btn-primary mt-3">
                                <i class="fas fa-calendar-plus me-2"></i>Browse Events
                            </a>
                        <% } else { %>
                            <div class="list-group">
                                <% events.forEach(event => { 
                                      const isPast = new Date(event.eventdatetimestart) < new Date();
                                %>
                                  <div class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                      <h6 class="mb-1"><%= event.eventname %></h6>
                                      <p class="mb-1 text-muted">
                                        <i class="fas fa-calendar me-2"></i>
                                        <%= new Date(event.eventdatetimestart).toLocaleDateString() %>
                                      </p>
                                      <small class="text-muted">
                                        <i class="fas fa-map-marker-alt me-2"></i>
                                        <%= event.eventlocation || 'TBA' %>
                                      </small>
                                    </div>
                              
                                    <div class="d-flex flex-column align-items-end">
                                      <% if (!isPast) { %>
                                        <!-- Future event badge -->
                                        <span class="badge bg-success bg-register mb-2">
                                            <%= event.registrationstatus %>
                                        </span>
                              
                                        <% if (event.registrationstatus === 'Registered') { %>
                                          <form method="POST" action="/profile/unregister/<%= event.registrationid %>" onsubmit="return confirm('Are you sure you want to unregister from this event?');">
                                            <button type="submit" class="btn btn-outline-danger btn-xs">
                                              <i class="fas fa-times me-1"></i> Unregister
                                            </button>
                                          </form>
                                        <% } %>
                              
                                      <% } else { %>
                                        <!-- Past event dropdown -->
                                        <form method="POST" action="/profile/update-event-status/<%= event.registrationid %>">
                                          <select name="status" class="form-select form-select-sm custom-event-status mb-2" onchange="this.form.submit()">
                                            <option value="Attended" <%= event.registrationstatus === 'Attended' ? 'selected' : '' %>>Attended</option>
                                            <option value="No-Show" <%= event.registrationstatus === 'No-Show' ? 'selected' : '' %>>No-Show</option>
                                            <option value="Cancelled" <%= event.registrationstatus === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                                          </select>
                                        </form>
                                      <% } %>
                                    </div>
                                  </div>
                                <% }) %>
                              </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
